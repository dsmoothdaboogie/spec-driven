# ui-app (Frontend)

Consumes typed contracts from `product-specs` (via `@org/contracts`, submodule, or release asset).
Implements views and clients consistent with OpenAPI; CI enforces contract alignment.
