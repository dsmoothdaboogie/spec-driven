# api-mw (Backend/Middleware)

Consumes contracts from `product-specs` (via `@org/contracts`, submodule, or release asset).
Implements endpoints consistent with OpenAPI; CI enforces spec gates and contract tests.
