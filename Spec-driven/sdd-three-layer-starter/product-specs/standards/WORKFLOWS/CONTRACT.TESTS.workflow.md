# CONTRACT.TESTS.workflow.md
Create contract tests that:
- Validate server responses against `contracts/openapi.yaml` (api-mw).
- Validate generated client types match the schema (ui-app).
- Include negative/error-model cases per SPEC.
