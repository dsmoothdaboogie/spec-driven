# SCAFFOLD.workflow.md
Using SPEC, PLAN, and `contracts/openapi.yaml`:
- Propose a file/folder scaffold (with brief purpose).
- Generate TypeScript interfaces & validation stubs aligning with OpenAPI.
- Emit test skeletons tracing to acceptance tests.
- Do not add new dependencies beyond `package.json` without approval.
