# Plan 04 — Collections & Environments
## Tasks
- P04-T1: `EnvironmentProvider` context (active env, variable resolution, secrets flag).
- P04-T2: Collections tree (add folder, add request, rename, delete) with local persistence.
- P04-T3: Save/Load JSON (export redacts secrets).
- P04-T4: History list with re-run support.
## Acceptance Mapping
- Env substitution works; collections persist; secrets redacted on export.
