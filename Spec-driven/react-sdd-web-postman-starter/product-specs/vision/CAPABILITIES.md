# Capabilities
- Request Builder (method, URL, headers, auth, body, query params).
- Environments & Variables with templating ({{var}}).
- Collections & Folders (save, export/import sans secrets).
- History & Favorites with persistence.
- Code Snippets (curl, fetch, axios).
- Proxy via middleware for CORS, TLS, allow-list, and cert management.
