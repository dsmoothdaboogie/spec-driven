name: product-specs-ci
on: [pull_request, push]
jobs:
  spec-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - name: Enforce plan/lineage updates
        run: bash scripts/spec_diff_check.sh origin/main
